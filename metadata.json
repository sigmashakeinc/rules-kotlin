{
  "description": "Kotlin/Android security governance rules for AI coding agents. Blocks force non-null assertions that cause NPEs, SQL string interpolation, Java deserialization (RCE), command injection via Runtime.exec(), hardcoded credentials, unsafe unchecked casts, and cleartext HTTP in production."
}
